name: Build X-MILI Integrated Binary

on:
  push:
    tags: ["v*"]
    paths:
      - 'xmili-integration/**'
      - 'x-mili/**'
      - '.github/workflows/xmili-binary-release.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goarch: [amd64, arm64]
    permissions:
      contents: write

    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'x-mili/go.mod'

      - name: Apply aimili-vpngate integration patches
        run: |
          cp xmili-integration/vpngate_aimili.go x-mili/web/service/
          cd x-mili
          patch -p1 < ../xmili-integration/vpngate.patch

      - name: Download Go dependencies
        run: |
          cd x-mili
          go mod download

      - name: Setup cross-compiler (arm64)
        if: matrix.goarch == 'arm64'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq gcc-aarch64-linux-gnu

      - name: Build X-MILI binary (${{ matrix.goarch }})
        run: |
          cd x-mili
          if [ "${{ matrix.goarch }}" = "arm64" ]; then
            export CC=aarch64-linux-gnu-gcc
          fi
          CGO_ENABLED=1 GOOS=linux GOARCH=${{ matrix.goarch }} \
            go build -ldflags "-w -s" -o ../x-mili-integrated main.go

      - name: Package binary
        run: |
          chmod +x x-mili-integrated
          tar czf x-mili-integrated-linux-${{ matrix.goarch }}.tar.gz x-mili-integrated

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: xmili-binary-linux-${{ matrix.goarch }}
          path: x-mili-integrated-linux-${{ matrix.goarch }}.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: xmili-binary-*
          merge-multiple: true
          path: release

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*.tar.gz
          generate_release_notes: true
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
